/**
 * Next.js configuration
 *
 * We do not specify any experimental options here. The `app` directory
 * is the default in modern Next.js versions and does not require
 * setting `experimental.appDir`. Keeping the config empty avoids
 * warnings about invalid options during build.
 * @type {import('next').NextConfig}
 */
const nextConfig = {};

module.exports = nextConfig;